doctype html
html(lang="ko")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href="/public/css/app.css")
        script.
            const roomId = "#{roomId}";
        title kakaozoom room 
        
    body 

        #container
            video#localVideo(autoplay, playsinline, width="500", height="500")
            video#remoteVideo(autoplay, playsinline, width="500", height="500")
            div#channel
                div#messageHistory
                input#messageBox(type="text")
                button#sendButton send 
            select#availableCameras
            
        script(src="https://webrtc.github.io/adapter/adapter-latest.js") 
        script(src="/socket.io/socket.io.js") 
        script(src="/public/js/app.js") 